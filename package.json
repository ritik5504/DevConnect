{
  "name": "devconnect-monorepo",
  "version": "1.0.0",
  "description": "DevConnect - Monorepo root configuration for deployment",
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm install --prefix backend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
